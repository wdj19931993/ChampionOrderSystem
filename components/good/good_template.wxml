<template name="good">
  <div class="good" bindtap="good_click"> 
    <view class="name">{{ name }}</view>
    <view class="price">{{ price }}</view>
    <view class="description">{{ description }}</view>
    <image style="background-color: #eeeeee;" src="{{image}}"></image>
  </div>
</template>