<template name="good">
  <div class="good"> 
    <view>{{ name }}</view>
    <view>{{ price }}</view>
    <view>{{ description }}</view>
    <image style="width: 200px; height: 200px; background-color: #eeeeee;" src="{{image}}"></image>
  </div>
</template>